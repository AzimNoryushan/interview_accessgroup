const config = {
    db: {
        database: 'volcano',
        host: 'localhost',
        user: 'root',
        password: '$r00tZ',
        
        
    },
    table: 'users',
    listPerPage: 10
};

module.exports = config;